{
  "name": "tfhe-matching-service",
  "version": "0.1.0",
  "description": "Phantom internal-match FHE compute service (node-tfhe). Hosts TFHE keys, exposes /encrypt and /internal-match/compare. Privacy-critical: no plaintext order amount/price leaves this service.",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test --test-reporter=spec test/"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ethers": "^6.13.0",
    "express": "^4.19.2",
    "node-tfhe": "^1.6.1"
  }
}
