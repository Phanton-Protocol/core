.vercel

# Pinned joinsplit_public9 wasm + circuit_final.zkey are committed under circuits/
# so Vercel builds succeed when Root Directory is this backend folder (see copy-joinsplit-prover-artifacts.mjs).
